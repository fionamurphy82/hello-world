function changeTheme(){

    let colours = ["red", "green", "blue"];

    for(i = 0; i < colours.length; i++){
        //console.log(colours[i]);
        document.body.style.backgroundColour = colours[i];
        }
}